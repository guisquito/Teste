{
  "name": "dashboard-tributario",
  "version": "4.0.0",
  "description": "Dashboard Tributário — 3 Empresas",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed'",
    "ios": "npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
